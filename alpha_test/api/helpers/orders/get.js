module.exports = {
  inputs: {
  },

  exits: {
    success: {
      outputFriendlyName: 'yuBOUGHT?'
    },

    recordNotFound: {
      description: 'there is no Order matching the search criteria'
    }
  },

  fn: async function(inputs,exits){
      
  }
}
